---
sidebar_position: 1
title: 前端性能优化概述
slug: /performance
---

# 🚀 前端性能优化

> **"每一毫秒都是钱"** —— Google 研究表明，页面加载时间每增加 1 秒，转化率下降 7%。

## 为什么性能优化如此重要？

想象一下这个场景：

```
用户打开你的网站...
  ↓
等待 1 秒... 还行
  ↓
等待 2 秒... 有点烦
  ↓
等待 3 秒... 关掉了 😤
```

**残酷的现实：**

- 53% 的移动用户会放弃加载超过 3 秒的页面
- Amazon 发现每 100ms 的延迟会让他们损失 1% 的销售额
- Pinterest 的搜索引擎流量和注册量增加了 15%，他们将等待时间减少了 40%

## 性能优化的三个阶段

```
┌─────────────────────────────────────────────────────────────┐
│                      性能优化全景图                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   📦 构建阶段          🌐 网络阶段          🖥️ 渲染阶段      │
│   ───────────         ──────────          ──────────       │
│   • 代码压缩          • 资源加载           • 关键渲染路径     │
│   • Tree Shaking      • 缓存策略           • 重排重绘        │
│   • 代码分割          • CDN 加速           • 合成层          │
│   • 图片优化          • 预加载             • 事件处理        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 核心性能指标

### Web Vitals（核心指标）

> **重要更新**：Google 于 2024 年 3 月正式以 INP 取代 FID 作为核心 Web Vitals 指标。

| 指标     | 全称                      | 衡量什么           | 目标值  | 状态                     |
| -------- | ------------------------- | ------------------ | ------- | ------------------------ |
| **LCP**  | Largest Contentful Paint  | 最大内容绘制时间   | < 2.5s  | 核心指标                 |
| **INP**  | Interaction to Next Paint | 所有交互的响应延迟 | < 200ms | 核心指标（2024 新增）    |
| **CLS**  | Cumulative Layout Shift   | 累计布局偏移       | < 0.1   | 核心指标                 |
| ~~FID~~  | First Input Delay         | 首次输入延迟       | < 100ms | ⚠️ 已弃用（被 INP 取代） |
| **TTFB** | Time to First Byte        | 首字节时间         | < 800ms | 辅助指标                 |

### 如何测量？

```javascript
// 使用 Web Vitals 库（v4+）
import {onLCP, onINP, onCLS} from 'web-vitals';

onLCP(console.log); // 最大内容绘制
onINP(console.log); // 交互响应（取代 FID）
onCLS(console.log); // 布局偏移
```

## 性能优化检查清单

### ✅ 加载性能

- [ ] 首屏加载时间 < 2 秒
- [ ] 关键 CSS 内联
- [ ] JavaScript 异步加载
- [ ] 图片懒加载
- [ ] 资源压缩（Gzip/Brotli）

### ✅ 渲染性能

- [ ] 避免强制同步布局
- [ ] 使用 transform/opacity 做动画
- [ ] 减少重排重绘
- [ ] 使用 will-change 提示浏览器

### ✅ 交互性能

- [ ] 首次输入延迟 < 100ms
- [ ] 长任务拆分（> 50ms）
- [ ] 使用 requestIdleCallback
- [ ] 虚拟列表优化大数据渲染

### ✅ 网络性能

- [ ] 合理的缓存策略
- [ ] HTTP/2 或 HTTP/3
- [ ] CDN 加速
- [ ] DNS 预解析

## 性能优化工具

| 工具                | 用途         | 特点                       |
| ------------------- | ------------ | -------------------------- |
| **Lighthouse**      | 综合评估     | Chrome 内置，一键生成报告  |
| **Chrome DevTools** | 性能分析     | 火焰图、网络面板、内存分析 |
| **WebPageTest**     | 真实环境测试 | 多地点、多设备             |
| **Bundle Analyzer** | 打包分析     | 可视化依赖关系             |
| **Performance API** | 代码埋点     | 精确测量特定操作           |

## 本专题内容

```
📁 性能优化
├── 📄 概述（本页）
├── 📄 加载性能优化
│   ├── 资源压缩与合并
│   ├── 代码分割与懒加载
│   ├── 图片优化策略
│   └── 预加载与预获取
├── 📄 渲染性能优化
│   ├── 关键渲染路径
│   ├── 重排重绘优化
│   └── 合成层与 GPU 加速
├── 📄 网络性能优化
│   ├── 缓存策略
│   ├── CDN 加速
│   └── HTTP/2 & HTTP/3
├── 📄 框架性能优化
│   ├── React 性能优化
│   ├── Vue 性能优化
│   └── 通用优化策略
├── 📄 性能监控
│   ├── 性能指标采集
│   ├── 性能监控平台
│   └── 性能优化实践
└── 📄 面试题精选
```

## 开始学习

准备好成为性能优化大师了吗？让我们从 **加载性能优化** 开始！

import DocCardList from '@theme/DocCardList';

<DocCardList />
